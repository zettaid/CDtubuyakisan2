
ユーザーのマイページ
<%= @user.first_name %>
<%= @user.last_name %>
<%= @user.post_number %>
<%= @user.address %>
<%= @user.phone_number %>
<%= @email %>


		<% if @user == current_user %>
        		<%= link_to "編集", edit_public_user_path(@user.id), class: "edit_#{@user.id}" %>
        <% end %>

        <% if @user == current_user %>

        	   <%= link_to "退会", root_path(@user.id), class: "destroy_#{@user.id}", method: :delete, "data-confirm" => "本当に消しますか？" %>
        <% end %>
